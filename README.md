## Model File Notice

본 프로젝트는 Stable Diffusion 기반 이미지 생성 모델인 SD 1.5를 사용합니다.

다만 모델 가중치 파일인 `.safetensors` 파일은 용량이 매우 커서 GitHub 저장소에는 포함하지 않았습니다.  
GitHub의 일반 파일 업로드 제한 및 저장소 용량 문제로 인해, 모델 파일은 별도로 관리해야 합니다.

따라서 이 프로젝트를 실행하려면 SD 1.5모델에 맞는 `.safetensors` 파일을 직접 추가해야 합니다.

```txt
my_model.safetensors
